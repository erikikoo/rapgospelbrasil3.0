<p id="notice"><%= notice %></p>

<h1>Listing Commitments</h1>

<table>
  <thead>
    <tr>
      <th>Artist data</th>
      <th>Local</th>
      <th>Endereco</th>
      <th>Numero</th>
      <th>Telefone</th>
      <th>Site</th>
      <th>Data</th>
      <th>Hora</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @commitments.each do |commitment| %>
      <tr>
        <td><%= commitment.artist_data %></td>
        <td><%= commitment.local %></td>
        <td><%= commitment.endereco %></td>
        <td><%= commitment.numero %></td>
        <td><%= commitment.telefone %></td>
        <td><%= commitment.site %></td>
        <td><%= commitment.data %></td>
        <td><%= commitment.hora %></td>
        <td><%= link_to 'Show', commitment %></td>
        <td><%= link_to 'Edit', edit_commitment_path(commitment) %></td>
        <td><%= link_to 'Destroy', commitment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Commitment', new_commitment_path %>
