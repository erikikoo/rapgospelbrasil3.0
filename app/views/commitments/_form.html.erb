<% if !@status.nil? %>  
    <%= alert(@status, 'Opps! Ocorreu um erro')%>  
<% end %>
<%= simple_form_for(@commitment, remote: true) do |f| %>
  
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :local, error: 'Campo obrigatório', autofocus: true %>
    <%= f.input :endereco, error: 'Campo obrigatório' %>
    <%= f.input :numero, error: 'Campo obrigatório' %>
    <%= f.input :telefone, error: 'Campo obrigatório' %>
    <%= f.input :site %>
    <%= f.input :data, start_year: Date.today.year %>
    <%= f.input :hora %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Criar Show", class: 'btn btn-default', id: 'enviar' %>
  </div>
<% end %>
