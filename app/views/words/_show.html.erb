

  <h3><%= @word.titulo %></h3>


<p>
   <%= raw(@word.texto) %>

   <span class="text-right">
   	<strong>Criando em : </strong><%= @word.created_at.strftime('%d/%m/%Y') %>
   </span>
</p>
<% if @status == 'adm'  %>
	<%= link_to 'Edit', "/edit_palavra/#{@word.id}/adm", class: 'btn btn-default', remote: true %> |
	<%= link_to 'Back', '/palavras/adm', class: 'btn btn-default', remote: true %>
<% end %>