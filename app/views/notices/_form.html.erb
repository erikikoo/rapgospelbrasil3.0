<%= simple_form_for(@notice, remote: true) do |f| %>
  <%= f.error_notification %>
	
  <div class="form-inputs">
    <%= f.input :titulo, autofocus: true %>
    <%= f.file_field(:logo) %>
    <%= f.input :noticia, :as => :ckeditor, :input_html => { :ckeditor => {:toolbar => 'mini', :cols => 100} } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, 'Criar Notícia' %>
  </div>
<% end %>



