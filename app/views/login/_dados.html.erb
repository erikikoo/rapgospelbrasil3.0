

<div class="dados-login">
  <% if artist_signed_in? %>  	       
      <span class="text-perfil">
        Você esta logado como <%= current_artist.admin? ? "Administrador" : "Artista"  %>
      </span>  

      <div class="btn-group">       
        
        <a class="btn btn-default" href=""><i class="icon-user icon-white"></i> <%= current_artist.email %> </a>
        <a class="btn btn-default dropdown-toggle" data-toggle="dropdown" href="#"><span class="caret"></span></a> 
        
        <ul class="dropdown-menu">
          <li>
            <%= link_to destroy_artist_session_path, method: :delete do %>
              <i class='glyphicon glyphicon-log-out'> Logout</i>
            <% end %>
          </li>
          <%if !current_artist.admin? %>   
              <li>
                <%= link_to principal_path do %>
                  <i class='glyphicon glyphicon-user'> Perfil</i>
                <% end %>
              </li>
          <% end %>
            <% if current_artist.admin? %>              
              <li>
              <%= link_to admin_index_path do %>
                <i class='glyphicon glyphicon-user'> Painel Administrador</i>
              <% end %>
            </li>
            <% end %>
             <li>
              <%= link_to root_path do %>
                <i class='glyphicon glyphicon-user'> Site</i>
              <% end %>   
            </li>        
                  
      </ul>
    </div>      
    <% else %>
    	
      <%= link_to 'Login', artist_session_path %> 
         
    <% end %>
  </div>