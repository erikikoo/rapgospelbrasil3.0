<%= simple_form_for(@phone, remote: true) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
      <%= f.input :telefone, autofocus: true%>
  </div>

  <div class="form-actions">
    <%= f.button :submit, 'Inserir Numero' %>
  </div>
<% end %>
